# API keys for LLM agents (used by docker compose for agent containers)
ANTHROPIC_API_KEY=sk-ant-...

# Path to Codex CLI auth.json (GPT agent uses ChatGPT auth, not an API key)
# Default: ~/.codex/auth.json
CODEX_AUTH_JSON=~/.codex/auth.json
